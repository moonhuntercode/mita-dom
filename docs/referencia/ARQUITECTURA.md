# Arquitectura Core

> 🚧 **Work in Progress:** Este documento detallará la estructura interna del paquete `mita-dom`, cómo interactúa el Proxy reactivo con el DOM y el ciclo de vida de `MitaElement`.
